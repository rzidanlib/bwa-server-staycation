<%- include('../../../partials/header.ejs') %>

<!-- Page Wrapper -->
<div id="wrapper">

  <%- include('../../../partials/sidebar.ejs') %>


  <!-- Content Wrapper -->
  <div id="content-wrapper" class="d-flex flex-column">

    <!-- Main Content -->
    <div id="content">

      <%- include('../../../partials/navbar.ejs') %>

      <!-- Begin Page Content -->
      <div class="container-fluid">

        <!-- Page Heading -->
        <h1 class="h3 mb-4 text-gray-800">Detail Item</h1>
        <%- include('../../../partials/message.ejs') %>

        <nav>
          <div class="nav nav-tabs" id="nav-tab" role="tablist">
            <a class="nav-item nav-link active" id="show-feature-tab" data-toggle="tab" href="#show-feature" role="tab" aria-controls="show-feature" aria-selected="true">Show Feature</a>
            <a class="nav-item nav-link" id="show-activity-tab" data-toggle="tab" href="#show-activity" role="tab" aria-controls="show-activity" aria-selected="false">Show Activity</a>
          </div>
        </nav>
        <div class="tab-content" id="nav-tabContent">
          <%- include('./show_feature.ejs') %>
          <%- include('./show_activity.ejs') %>
        </div>
      </div>
      <!-- /.container-fluid -->

    </div>
    <!-- End of Main Content -->

    <%- include('../../../partials/footer.ejs') %>


  </div>
  <!-- End of Content Wrapper -->

</div>
<!-- End of Page Wrapper -->


<%- include('./show_modal_activity.ejs') %>
<%- include('./show_modal_feature.ejs') %>
<%- include('../../../partials/js.ejs') %>
<script>
  $(document).ready(function() {
    $('#dataTableActivity').DataTable()
  })
  CKEDITOR.replace('about');

  $('#dataTableActivity').on('click', '.button-update', function() {
    let id = $(this).data('id')
    let name = $(this).data('name')
    let type = $(this).data('type')
    $('#editModalActivity').modal('show')
    $('.id').val(id)
    $('.name').val(name)
    $('.type').val(type)
  })

  $('#dataTable').on('click', '.button-update', function() {
    let id = $(this).data('id')
    let name = $(this).data('name')
    let qty = $(this).data('qty')
    $('#editModal').modal('show')
    $('.id').val(id)
    $('.name').val(name)
    $('.qty').val(qty)
  })
</script>